# ============================================================================
# aidp-mock-rubik values
# ============================================================================
# pathPrefix MUST stay /rubik to match apps.rubik.path_prefix=/rubik/ in the
# iam DB (seeded by keycloak-init).
# ============================================================================

namespace: mock-rubik
image: mock-rubik:v1
pullPolicy: IfNotPresent
replicas: 1
port: 8081
pathPrefix: /rubik
