Gateway详细测试用例

来源文件：
dttest/gateway-test-design-xmind.txt

说明：
1、本文件只覆盖Gateway测试方案中的GW和BIP两类内容。
2、IAM认证、JWT、ACL、API Key、资源鉴权不纳入本文件。
3、whoami表示测试用HTTP echo后端，可替换为任意返回明确HTTP响应的测试服务。建议响应体包含固定服务标识、请求路径和请求头回显；如需证明请求是否到达后端，可使用测试后端提供的黑盒命中计数接口。
4、BIP类用例依赖DM前端网络平面、平台网络入口、业务IP分配和保IP能力。
5、本文件的通过条件优先使用外部客户端可观察行为，包括安装/配置命令返回、入口连通性、HTTP状态码、响应体、TLS握手结果、限流响应和业务路径恢复结果；Kubernetes内部资源状态不作为用例通过的主判断依据。


一、GW：应用初始化与路由集成

名称：部署后入口地址正常暴露
编号：DEPLOY-TC-001
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、IAM与Gateway已通过同一部署流程完成首次部署，Helm release状态为成功。
3、测试客户端与Gateway入口网络可达，集群内客户端可访问IAM服务地址。
测试步骤：
1、根据部署参数记录Gateway对外入口地址和端口，同时记录IAM服务或登录入口地址。
2、从外部测试客户端向Gateway入口发起TCP/HTTP连接请求。
3、从集群内客户端访问IAM健康检查或登录入口，记录HTTP状态码。
4、访问Gateway未配置路径和已配置whoami测试路径，分别记录响应。
预期结果：
1、Gateway入口地址、端口以及IAM服务入口可从部署输出或交付配置中明确获得。
2、外部客户端连接Gateway入口成功，不出现连接拒绝或连接超时。
3、IAM服务入口可访问，返回健康检查成功、登录页面或等价可用响应。
4、Gateway未配置路径返回未匹配响应，已配置测试路径返回后端真实响应。

名称：重复部署保持幂等
编号：DEPLOY-TC-002
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、IAM与Gateway已完成首次部署，Gateway入口可访问，IAM服务可访问。
3、已发布一条可访问的whoami测试路由。
测试步骤：
1、访问IAM入口、Gateway未配置路径和whoami测试路径，记录当前HTTP状态码、响应体和入口地址。
2、使用相同参数再次执行IAM与Gateway部署或升级命令，等待命令完成。
3、再次访问步骤1中的IAM入口、Gateway未配置路径和whoami测试路径，记录HTTP状态码和响应体。
4、对比重复部署前后的入口地址、未匹配响应、IAM响应和测试后端响应。
预期结果：
1、重复部署命令执行成功，无报错退出。
2、重复部署前后Gateway入口地址和IAM入口地址保持一致。
3、Gateway未配置路径仍返回未匹配响应，whoami测试路径仍返回HTTP 200。
4、IAM入口仍可访问，Gateway入口能力无退化，不需要重新发布业务路由。

名称：发布测试路由后Gateway能够转发到测试后端
编号：GW-TC-006
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署且入口可访问。
3、whoami或等价测试后端已部署。
测试步骤：
1、发布指向测试后端的路由配置，等待路由生效窗口。
2、从外部客户端访问该路由路径，记录HTTP状态码和响应体。
3、通过测试后端的黑盒命中计数接口确认请求到达情况。
预期结果：
1、路由发布命令或配置接口返回成功。
2、外部访问返回测试后端真实响应，HTTP状态码为200。
3、响应体包含后端标识或请求路径回显。
4、后端命中计数增加，证明请求已到达后端。

名称：已生效业务路径外部访问返回后端真实响应
编号：GW-TC-007
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，whoami后端已部署，测试业务路径已发布并完成生效等待。
测试步骤：
1、从外部客户端访问业务路径，记录HTTP响应内容。
2、检查响应内容是否包含后端标识信息，如whoami返回的主机名、请求路径或请求头回显。
3、重复访问3次，记录结果是否稳定。
预期结果：
1、业务路径稳定返回HTTP 200。
2、响应内容为测试后端真实响应，包含后端标识。
3、多次访问结果一致，不出现间歇性404、502、503或连接超时。

名称：后端不可用时路由异常可被外部访问感知
编号：GW-TC-008
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署。
3、准备一条指向不可用后端的测试路由，同时保留一条正常后端路由。
测试步骤：
1、发布指向不可用后端的测试路由，等待路由生效窗口。
2、从外部客户端访问异常路由路径，记录HTTP返回码和响应体。
3、从外部客户端访问正常后端路由，记录HTTP返回码和响应体。
预期结果：
1、异常路由不返回正常后端响应，返回502、503或等价错误响应。
2、错误响应可被外部客户端明确感知，不表现为无限等待。
3、正常后端路由仍返回HTTP 200，Gateway整体不因单条异常路由不可用。

名称：删除业务路由后原路径不再转发
编号：GW-TC-009
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，业务路由已配置且路径可正常访问。
3、测试后端提供黑盒命中计数接口。
测试步骤：
1、访问业务路径，记录HTTP响应和后端命中计数，确认删除前路由可用。
2、删除对应业务路由配置，等待路由失效窗口。
3、再次访问原业务路径，记录HTTP返回码和响应体。
4、再次读取后端命中计数。
预期结果：
1、删除前业务路径访问正常，返回后端响应。
2、删除操作返回成功。
3、删除后原路径返回404或等价未匹配响应，不返回后端内容。
4、删除后后端命中计数不增加，请求不再进入后端。

名称：跨namespace引用后端并完成授权后请求可转发
编号：GW-TC-010
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试路由和后端服务位于不同namespace。
3、测试人员具备发布跨namespace授权配置的权限。
测试步骤：
1、发布跨namespace引用后端的测试路由，但暂不发布授权配置。
2、从外部客户端访问跨namespace路径，记录HTTP响应。
3、发布允许该跨namespace引用的授权配置，等待生效窗口。
4、再次从外部客户端访问同一路径，记录HTTP响应和后端命中计数。
预期结果：
1、授权配置发布前，请求不能正常到达后端，返回404、502、503或等价错误响应。
2、授权配置发布后，同一路径返回HTTP 200和后端真实响应。
3、后端命中计数增加，跨namespace转发生效。

名称：缺失跨namespace授权时请求不转发
编号：GW-TC-011
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试路由跨namespace引用后端服务。
3、后端namespace未发布允许该引用的授权配置。
4、测试后端提供黑盒命中计数接口。
测试步骤：
1、记录测试后端当前命中计数。
2、从外部客户端访问跨namespace路径，记录HTTP返回码和响应体。
3、再次读取测试后端命中计数。
预期结果：
1、外部请求不能到达后端，返回404、502、503或等价错误响应。
2、响应中不包含后端业务内容。
3、后端命中计数不增加，请求被Gateway拦截或未被转发。

名称：删除跨namespace授权后已存在路由变为不可用
编号：GW-TC-012
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、跨namespace业务路径已正常工作，对应授权配置已存在。
3、测试后端提供黑盒命中计数接口。
测试步骤：
1、访问跨namespace路径，记录HTTP响应和后端命中计数，确认删除前路由可用。
2、删除对应跨namespace授权配置，等待生效窗口。
3、再次访问同一路径，记录HTTP返回码和响应体。
4、再次读取后端命中计数。
预期结果：
1、删除前请求可正常转发到后端，返回HTTP 200。
2、授权配置删除操作返回成功。
3、删除后路由变为不可用，访问返回404、502、503或等价错误响应。
4、删除后后端命中计数不增加，请求不再进入后端。

名称：重新补齐跨namespace授权后路由自动恢复
编号：GW-TC-013
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、跨namespace业务路径已存在，当前缺少授权配置且访问不可用。
3、测试后端提供黑盒命中计数接口。
测试步骤：
1、访问跨namespace路径，确认当前返回错误响应。
2、重新发布跨namespace授权配置，等待生效窗口。
3、再次从外部客户端访问同一路径，记录HTTP响应和后端命中计数。
预期结果：
1、授权配置补齐前路径不可用，不返回后端业务内容。
2、授权配置发布成功。
3、授权配置补齐后外部访问恢复正常，返回HTTP 200和后端响应。
4、后端命中计数增加，路由能力自动恢复。

名称：IP白名单允许来源访问成功
编号：GW-TC-014
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，基础路由已可用。
3、Gateway入口能识别真实客户端源IP，已准备白名单来源客户端。
测试步骤：
1、配置允许指定来源CIDR的IP白名单策略，等待策略生效窗口。
2、从白名单来源客户端访问受保护路径，记录HTTP返回码和响应体。
3、读取测试后端命中计数。
预期结果：
1、IP白名单策略配置命令返回成功。
2、白名单来源请求被Gateway放行，返回后端正常响应，HTTP状态码为200。
3、响应体包含后端标识，后端命中计数增加。

名称：非白名单来源访问被Gateway拒绝
编号：GW-TC-015
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，IP白名单策略已配置。
3、已准备非白名单来源客户端，测试后端提供黑盒命中计数接口。
测试步骤：
1、记录测试后端当前命中计数。
2、从非白名单来源客户端访问受保护路径，记录HTTP返回码和响应体。
3、再次读取测试后端命中计数。
预期结果：
1、请求被Gateway拒绝，返回403或等价拒绝类状态码，不返回后端业务内容。
2、后端命中计数不增加，请求在Gateway层被拦截，未到达后端。

名称：删除IP白名单策略后基础路由恢复无白名单限制
编号：GW-TC-016
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，基础路由已可用，IP白名单策略已配置。
测试步骤：
1、删除IP白名单策略，等待策略失效窗口。
2、从原非白名单来源或普通客户端访问业务路径，记录HTTP返回码和响应体。
3、读取测试后端命中计数。
预期结果：
1、IP白名单策略删除操作返回成功。
2、业务路径恢复正常访问，返回后端响应，HTTP状态码为200。
3、响应体包含后端标识，后端命中计数增加，无白名单限制。

名称：入口无法保留真实源IP时IP白名单测试记录环境约束
编号：GW-TC-017
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，当前入口可能存在SNAT。
3、测试后端响应可回显Gateway看到的客户端IP。
测试步骤：
1、从测试客户端访问测试后端路径，记录HTTP响应中的来源IP信息。
2、对比响应中的来源IP与测试客户端真实IP，判断是否一致。
3、若来源IP与真实客户端IP不一致，在测试记录中说明SNAT或不保源IP限制。
预期结果：
1、若Gateway能识别真实客户端IP，来源IP与测试客户端IP一致，IP白名单测试结论有效。
2、若Gateway无法识别真实客户端IP，测试记录中说明SNAT或不保源IP限制，IP白名单拦截结果不作为最终结论。
3、后续切换业务IP、保源IP入口或等价网络方案后重测，记录重测结论。

名称：路径白名单内路径访问成功
编号：GW-TC-018
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，whoami或等价测试后端已部署。
3、仅发布指定白名单路径的业务路由。
测试步骤：
1、从外部客户端访问白名单内路径，记录HTTP响应。
2、读取测试后端命中计数。
预期结果：
1、白名单内路径请求成功转发到后端，返回后端正常响应，HTTP状态码为200。
2、响应体包含后端标识，后端命中计数增加。

名称：路径白名单外路径默认拒绝
编号：GW-TC-019
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，whoami或等价测试后端已部署。
3、仅发布指定白名单路径的业务路由，白名单外路径无路由配置。
4、测试后端提供黑盒命中计数接口。
测试步骤：
1、记录测试后端当前命中计数。
2、从外部客户端访问白名单外路径，记录HTTP返回码和响应体。
3、再次读取测试后端命中计数。
预期结果：
1、白名单外路径返回404、403或等价拒绝响应，不返回后端业务内容。
2、后端命中计数不增加，请求未被误转发到后端。

名称：未知路径默认不转发到任意后端
编号：GW-TC-020
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，存在至少一个可用测试后端。
3、未知路径未配置任何业务路由。
4、各测试后端提供黑盒命中计数接口。
测试步骤：
1、记录所有相关测试后端当前命中计数。
2、从外部客户端访问未知路径，记录HTTP返回码和响应体。
3、再次读取所有相关测试后端命中计数。
预期结果：
1、未知路径返回404或等价未匹配响应，不返回任何后端业务内容。
2、所有测试后端命中计数均不增加，请求未被误转发到任意后端。

名称：新增路径白名单后新路径可访问
编号：GW-TC-021
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试后端已部署。
3、待新增路径当前不可访问。
测试步骤：
1、访问待新增路径，记录HTTP返回码，确认当前不转发。
2、更新业务路由配置，新增该路径，等待生效窗口。
3、再次访问新增路径，记录HTTP响应和后端命中计数。
预期结果：
1、新增前路径不可访问，返回404或等价未匹配响应。
2、路由更新操作返回成功。
3、新增后路径可访问，返回后端正常响应，HTTP状态码为200。
4、后端命中计数增加，说明新增路径已生效。

名称：请求超时策略生效后慢请求被Gateway终止
编号：GW-TC-026
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试后端已部署，并提供可控制响应延迟的接口，例如 `/delay/3`。
3、测试路由已配置 `HTTPRoute.rules[].timeouts.request` 或等价请求超时策略，超时时间小于后端延迟时间。
4、同时保留一条无延迟或延迟小于超时时间的正常测试路径。
测试步骤：
1、发布带请求超时的测试路由，等待路由生效窗口。
2、从外部客户端访问正常测试路径，记录HTTP状态码和响应体。
3、从外部客户端访问慢请求路径，记录客户端等待时间、HTTP状态码和响应体。
4、再次访问正常测试路径，确认基础路由仍可用。
预期结果：
1、正常测试路径返回HTTP 200和后端真实响应。
2、慢请求在配置的超时时间附近被Gateway终止，不会一直等待后端完成。
3、慢请求返回504、503或等价超时响应，不返回后端成功响应。
4、请求超时策略只影响目标路由或目标规则，不影响其他正常路径访问。

名称：请求次数限制策略达到阈值后触发限流
编号：GW-TC-027
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试后端已部署，基础测试路由可正常访问。
3、目标路由已绑定 `BackendTrafficPolicy` 或等价限流策略，阈值已知，例如每秒允许1次请求。
4、测试后端提供黑盒命中计数接口，或响应日志可用于确认到达后端的请求数量。
测试步骤：
1、记录测试后端当前命中计数。
2、以低于阈值的频率访问目标路径，记录HTTP状态码和响应体。
3、在一个限流窗口内连续发起多次请求，记录每次HTTP状态码。
4、读取测试后端命中计数，统计成功到达后端的请求数量。
5、等待限流窗口恢复后再次访问目标路径，记录HTTP状态码。
预期结果：
1、阈值内请求返回HTTP 200和后端真实响应。
2、超过阈值的请求返回429或设计规定的限流拒绝响应，不再全部返回200。
3、后端命中计数增长量不超过策略允许范围，超出阈值的请求未全部进入后端。
4、限流窗口恢复后，目标路径可重新返回HTTP 200。
5、该策略可作为基础DDoS防护能力验证，证明异常高频请求会在Gateway层被削减。

名称：请求IP白名单策略只允许白名单来源访问
编号：GW-TC-028
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试后端已部署，基础路由可正常访问。
3、Gateway入口可识别真实客户端源IP，或已配置可信 `X-Forwarded-For` 来源IP识别策略。
4、目标路由已绑定 `SecurityPolicy.authorization` 或等价IP白名单策略，默认动作为拒绝，仅允许指定来源CIDR。
5、已准备白名单来源客户端和非白名单来源客户端。
测试步骤：
1、记录测试后端当前命中计数。
2、从白名单来源客户端访问目标路径，记录HTTP状态码和响应体。
3、从非白名单来源客户端访问同一目标路径，记录HTTP状态码和响应体。
4、再次读取测试后端命中计数。
5、删除或关闭白名单策略后，从普通客户端再次访问目标路径。
预期结果：
1、白名单来源请求被Gateway放行，返回HTTP 200和后端真实响应。
2、非白名单来源请求被Gateway拒绝，返回403或等价拒绝响应，不返回后端业务内容。
3、后端命中计数只因白名单来源请求增加，非白名单来源请求不增加命中计数。
4、白名单策略删除或关闭后，基础路由恢复正常访问。
5、若测试环境存在SNAT导致无法识别真实源IP，需记录环境约束并切换保源IP入口重测。

名称：请求IP黑名单策略拒绝黑名单来源访问
编号：GW-TC-029
预置条件：
1、A800集群正常，模型服务、数据库、向量库服务集群正常。
2、Gateway已部署，测试后端已部署，基础路由可正常访问。
3、Gateway入口可识别真实客户端源IP，或已配置可信 `X-Forwarded-For` 来源IP识别策略。
4、目标路由已绑定 `SecurityPolicy.authorization` 或等价IP黑名单策略，默认动作为允许，拒绝指定来源CIDR。
5、已准备黑名单来源客户端和非黑名单来源客户端。
测试步骤：
1、记录测试后端当前命中计数。
2、从黑名单来源客户端访问目标路径，记录HTTP状态码和响应体。
3、从非黑名单来源客户端访问同一目标路径，记录HTTP状态码和响应体。
4、再次读取测试后端命中计数。
5、删除或关闭黑名单策略后，从原黑名单来源再次访问目标路径。
预期结果：
1、黑名单来源请求被Gateway拒绝，返回403或等价拒绝响应，不返回后端业务内容。
2、非黑名单来源请求被Gateway放行，返回HTTP 200和后端真实响应。
3、后端命中计数只因非黑名单来源请求增加，黑名单来源请求不增加命中计数。
4、黑名单策略删除或关闭后，原黑名单来源访问恢复正常，返回HTTP 200。
5、策略变更只影响目标路由，不影响其他业务路径。

名称：Gateway日志类型自动注册到OMS成功
编号：LOG-TC-001
预置条件：
1、A800集群正常，IAM与Gateway已部署成功。
2、OMS日志收集功能已部署完成，测试用户具备查看日志类型和发起日志收集任务的权限。
3、Gateway已按交付配置开启OMS日志对接能力。
测试步骤：
1、等待Gateway部署完成后进入OMS日志收集页面。
2、查看可收集组件或日志类型列表。
3、选择Gateway相关日志类型，检查页面是否允许发起日志收集任务。
预期结果：
1、OMS页面可看到Gateway日志收集对象，无需人工补录。
2、Gateway日志类型显示清晰，至少包含控制面、数据面、管理面、资源配置和事件类日志。
3、选择Gateway日志类型后可正常进入日志收集流程。
4、页面无注册失败、组件不可用或日志类型缺失提示。

名称：Gateway日志收集任务下发后按节点类型上传OMS
编号：LOG-TC-002
预置条件：
1、A800集群正常，Gateway已部署成功。
2、OMS日志收集页面可选择Gateway日志类型。
3、OMS日志归档目录或下载入口可用于检查收集结果。
测试步骤：
1、在OMS页面选择Gateway多个日志对象，设置日志收集时间范围。
2、发起Gateway日志收集任务并等待任务完成。
3、从OMS日志下载入口或归档目录获取本次日志包。
4、解压日志包并检查每个包内的内容。
预期结果：
1、OMS任务创建成功，Gateway日志收集任务可正常结束。
2、不同Gateway日志对象分别生成独立zip包，不混成一个总包。
3、日志包命名符合 `{nodeName}_{startTime}_{endTime}.zip`，如 `gateway-controller_20260512171500_20260513171500.zip`，文件名不包含空格。
4、上传到OMS目录的zip包可解压，包含收集时间、组件标识和对应日志对象的日志或资源内容。

名称：Gateway日志进度查询返回OMS可展示状态
编号：LOG-TC-003
预置条件：
1、LOG-TC-002已执行，存在Gateway日志收集任务。
2、OMS日志收集页面可查看任务进度和任务结果。
测试步骤：
1、在OMS页面发起一次Gateway日志收集任务。
2、任务执行过程中刷新或查看OMS任务进度。
3、任务结束后查看OMS页面展示的组件状态、节点状态和日志文件名称。
4、对比页面显示的文件名称与实际下载到的zip包文件名称。
预期结果：
1、OMS页面可展示Gateway日志收集进度，不出现一直卡在收集中。
2、任务完成后显示成功；如部分日志上传失败，页面可看到明确的部分失败状态和原因。
3、页面展示的日志对象名称不包含空格，文件名为对应日志对象的zip包名。
4、页面展示文件名与实际下载到的zip包文件名一致。

名称：IAM日志类型自动注册到OMS成功
编号：LOG-TC-004
预置条件：
1、A800集群正常，IAM已部署成功。
2、OMS日志收集功能已部署完成，测试用户具备查看日志类型和发起日志收集任务的权限。
3、IAM已按交付配置开启OMS日志对接能力。
测试步骤：
1、等待IAM部署完成后进入OMS日志收集页面。
2、查看可收集组件或日志类型列表。
3、选择IAM相关日志类型，检查页面是否允许发起日志收集任务。
预期结果：
1、OMS页面可看到IAM日志收集对象，无需人工补录。
2、IAM日志类型显示清晰，至少包含认证服务、鉴权代理、策略服务、Keycloak、数据库、资源配置和事件类日志。
3、选择IAM日志类型后可正常进入日志收集流程。
4、页面无注册失败、组件不可用或日志类型缺失提示。

名称：IAM日志收集任务下发后按节点类型上传OMS
编号：LOG-TC-005
预置条件：
1、A800集群正常，IAM已部署成功。
2、OMS日志收集页面可选择IAM日志类型。
3、OMS日志归档目录或下载入口可用于检查收集结果。
测试步骤：
1、在OMS页面选择IAM多个日志对象，设置日志收集时间范围。
2、发起IAM日志收集任务并等待任务完成。
3、从OMS日志下载入口或归档目录获取本次日志包。
4、解压日志包并检查每个包内的内容。
预期结果：
1、OMS任务创建成功，IAM日志收集任务可正常结束。
2、不同IAM日志对象分别生成独立zip包，不混成一个总包。
3、日志包命名符合 `{nodeName}_{startTime}_{endTime}.zip`，如 `iam-keycloak-proxy_20260512171500_20260513171500.zip`，文件名不包含空格。
4、上传到OMS目录的zip包可解压，包含收集时间、组件标识和对应日志对象的日志或资源内容。

名称：IAM日志进度查询返回OMS可展示状态
编号：LOG-TC-006
预置条件：
1、LOG-TC-005已执行，存在IAM日志收集任务。
2、OMS日志收集页面可查看任务进度和任务结果。
测试步骤：
1、在OMS页面发起一次IAM日志收集任务。
2、任务执行过程中刷新或查看OMS任务进度。
3、任务结束后查看OMS页面展示的组件状态、节点状态和日志文件名称。
4、对比页面显示的文件名称与实际下载到的zip包文件名称。
预期结果：
1、OMS页面可展示IAM日志收集进度，不出现一直卡在收集中。
2、任务完成后显示成功；如部分日志上传失败，页面可看到明确的部分失败状态和原因。
3、页面展示的日志对象名称不包含空格，文件名为对应日志对象的zip包名。
4、页面展示文件名与实际下载到的zip包文件名一致。

名称：首次部署指定前端网络平面后自动获取业务IP
编号：BIP-TC-001
预置条件：
1、A800容器平台正常，前端网络平面已创建且存在可用IP资源。
2、Gateway组件镜像、部署清单和部署参数已准备就绪。
3、网络管理员已提供前端网络平面标识。
4、已准备whoami或等价测试业务后端。
测试步骤：
1、在Gateway首次部署参数中填写前端网络平面标识并执行部署。
2、从容器平台部署输出或平台页面记录Gateway获得的业务IP。
3、使用该业务IP访问未配置路径，记录HTTP状态码和响应体。
4、发布一条测试业务路由，使用该业务IP访问测试路径，记录HTTP状态码和响应体。
预期结果：
1、Gateway部署命令执行成功。
2、Gateway自动获得业务IP，业务IP属于指定前端网络平面。
3、未配置路径返回404或等价未匹配响应，不出现连接拒绝或超时。
4、测试路径返回HTTP 200，响应体包含后端服务标识或请求路径回显。

名称：Gateway同时挂载前端与后端网络平面
编号：BIP-TC-002
预置条件：
1、A800容器平台正常，前端网络平面和后端网络平面均已创建。
2、Gateway已按前端网络平面标识完成部署并获得业务IP。
3、后端业务服务已部署在后端网络平面可达范围内。
4、已发布一条指向后端业务服务的测试路由。
测试步骤：
1、从外部客户端通过业务IP访问测试业务路径，记录HTTP响应。
2、从外部客户端尝试通过业务IP访问后端网络平面内部服务地址或内部管理路径。
3、从业务后端侧发起一次依赖后端网络平面的服务访问，记录业务响应是否正常。
预期结果：
1、外部客户端通过业务IP访问测试业务路径成功，返回HTTP 200和后端真实响应。
2、外部客户端不能通过业务IP直接访问后端网络平面内部服务。
3、Gateway对外走前端网络平面，对内转发后端业务流量正常。

名称：首次部署指定不存在的前端网络平面时失败可观察
编号：BIP-TC-003
预置条件：
1、A800容器平台正常。
2、准备一个不存在或无权限使用的前端网络平面标识。
3、Gateway尚未完成首次部署，或可在隔离测试环境执行失败部署。
测试步骤：
1、在Gateway部署参数中填写不存在或无权限的前端网络平面标识。
2、执行Gateway部署，记录部署命令或平台任务返回。
3、尝试访问预期业务IP或部署输出中可能产生的入口地址。
预期结果：
1、部署失败信息可观察，能明确指向前端网络平面不存在、无权限或不可用。
2、Gateway不应获得可对外访问的错误业务IP。
3、外部访问不返回Gateway后端业务响应。

名称：前端网络平面IP资源耗尽时部署失败可观察
编号：BIP-TC-004
预置条件：
1、A800容器平台正常。
2、准备一个IP资源已耗尽的前端网络平面。
3、Gateway部署参数可指定该前端网络平面。
测试步骤：
1、使用IP资源耗尽的前端网络平面标识执行Gateway部署。
2、记录部署命令或平台任务返回。
3、从外部客户端尝试访问本次部署预期业务入口，记录连接结果。
预期结果：
1、部署失败或业务IP分配失败信息可观察，能明确指向IP资源不足。
2、Gateway不应绑定到错误或未授权业务IP。
3、外部客户端无法通过本次失败部署访问后端业务。

名称：前端网络平面部署参数格式错误时被拒绝
编号：BIP-TC-005
预置条件：
1、A800容器平台正常。
2、准备格式错误的前端网络平面部署参数，例如空值、非法字符或不符合平台规范的标识。
3、如已有Gateway可用入口，应先记录其访问结果。
测试步骤：
1、使用格式错误的前端网络平面参数执行部署或升级。
2、记录部署命令或平台任务返回。
3、如存在变更前可用入口，再次访问该入口测试路径。
预期结果：
1、错误参数应被部署流程拒绝，错误信息可观察。
2、不会产生新的错误业务IP绑定。
3、若变更前已有可用入口，原入口仍返回HTTP 200和后端真实响应。

名称：运行期变更前端网络平面后新业务IP生效
编号：BIP-TC-006
预置条件：
1、Gateway已通过旧前端网络平面部署完成，旧业务IP访问测试路径正常。
2、已创建新的前端网络平面，且存在可用IP资源。
3、网络管理员已提供新前端网络平面标识。
测试步骤：
1、使用旧业务IP访问测试业务路径，记录HTTP响应。
2、修改Gateway部署参数中的前端网络平面标识为新标识并执行升级。
3、从部署输出或平台页面记录升级后的新业务IP。
4、使用新业务IP访问同一测试业务路径。
5、使用旧业务IP访问同一测试业务路径。
预期结果：
1、变更前旧业务IP返回HTTP 200和后端真实响应。
2、升级命令执行成功，新业务IP属于新的前端网络平面。
3、新业务IP返回HTTP 200和后端真实响应。
4、旧业务IP按变更策略下线、不可达或不再作为当前业务入口使用。

名称：业务IP变更失败时原业务IP保持可用
编号：BIP-TC-007
预置条件：
1、Gateway已有可用业务IP，测试业务路径访问正常。
2、准备一个不存在、无权限或IP资源耗尽的目标前端网络平面标识。
测试步骤：
1、使用原业务IP访问测试业务路径，记录HTTP响应。
2、尝试把Gateway前端网络平面变更为异常目标标识，记录升级结果。
3、在失败后继续使用原业务IP访问测试业务路径，记录HTTP响应。
4、确认是否产生了新的可访问业务IP。
预期结果：
1、变更前原业务IP返回HTTP 200和后端真实响应。
2、异常变更失败信息可观察。
3、失败后原业务IP仍返回HTTP 200，已有路由与鉴权能力不受影响。
4、不产生错误的新业务IP入口。

名称：业务IP变更支持滚动升级且业务不中断
编号：BIP-TC-008
预置条件：
1、Gateway以多副本方式部署，当前每个副本业务IP均可访问测试路径。
2、准备新的前端网络平面，具备足够IP资源。
3、外部客户端可持续访问Gateway业务入口。
测试步骤：
1、在变更前连续访问当前业务入口，记录基线成功率。
2、修改前端网络平面标识并执行滚动升级。
3、升级过程中持续访问旧业务入口、新业务入口或前置负载入口，记录HTTP状态码和响应体。
4、升级完成后访问新业务IP列表中的每个业务IP。
预期结果：
1、变更前业务访问稳定返回HTTP 200。
2、滚动升级过程中允许单次请求短暂失败，但整体业务不应长时间中断。
3、升级完成后新业务IP均返回HTTP 200和后端真实响应。
4、无需重新发布业务路由。

名称：多副本Gateway每副本独立持有业务IP
编号：BIP-TC-009
预置条件：
1、A800容器平台正常，前端网络平面IP资源数量满足Gateway副本数。
2、Gateway部署参数配置为多副本。
3、已发布一条whoami或等价测试业务路由。
测试步骤：
1、部署或扩容Gateway到多副本。
2、从平台输出或平台页面记录每个Gateway副本分配到的业务IP。
3、分别访问每个业务IP上的测试业务路径，记录HTTP状态码和响应体。
4、如配置DNS轮询或前置负载均衡，通过统一域名或统一入口连续访问多次。
预期结果：
1、每个Gateway副本都获得独立业务IP。
2、每个业务IP访问测试业务路径均返回HTTP 200和后端真实响应。
3、DNS轮询或前置负载均衡访问时，请求可稳定分流到可用业务IP。

名称：多副本扩容时新增副本自动获取业务IP
编号：BIP-TC-010
预置条件：
1、Gateway已以较低副本数部署并正常访问。
2、前端网络平面仍有可用IP资源。
3、已发布一条whoami或等价测试业务路由。
测试步骤：
1、记录扩容前已有业务IP列表和访问结果。
2、提升Gateway副本数并等待扩容完成。
3、从平台输出或平台页面记录扩容后的业务IP列表。
4、访问新增业务IP上的测试业务路径。
预期结果：
1、扩容前已有业务IP访问正常。
2、扩容后业务IP数量随副本数增加。
3、新增业务IP返回HTTP 200和后端真实响应。
4、已有业务IP仍保持可访问。

名称：Pod异常重建后原业务IP保持不变
编号：BIP-TC-011
预置条件：
1、Gateway已部署并获得业务IP，测试业务路径访问正常。
2、Gateway以支持稳定身份的部署形态运行。
3、具备模拟Gateway Pod异常重建的权限。
测试步骤：
1、记录目标Gateway副本的业务IP，并使用该IP访问测试业务路径。
2、模拟该Gateway Pod异常退出或被删除。
3、等待容器平台完成Pod重建。
4、再次记录该Gateway副本对应业务IP，并使用该IP访问测试业务路径。
预期结果：
1、Pod重建前业务IP返回HTTP 200和后端真实响应。
2、Pod重建后业务IP保持不变。
3、同一业务IP恢复HTTP 200和后端真实响应。
4、恢复过程无需人工重新绑定业务IP。

名称：节点故障触发Pod漂移后业务IP保持稳定
编号：BIP-TC-012
预置条件：
1、Gateway已部署并获得业务IP，测试业务路径访问正常。
2、容器平台支持节点故障后按Pod身份保留业务IP。
3、具备模拟Gateway所在节点故障或节点隔离的权限。
测试步骤：
1、记录故障前Gateway业务IP，并使用该IP访问测试业务路径。
2、模拟Gateway所在节点故障或节点隔离，触发Pod漂移到其他节点。
3、等待容器平台完成漂移和业务IP恢复。
4、再次使用原业务IP访问测试业务路径。
预期结果：
1、故障前原业务IP返回HTTP 200和后端真实响应。
2、节点故障后Gateway Pod可在新节点恢复服务。
3、原业务IP保持不变并恢复可访问。
4、客户端无需变更访问地址。

名称：单副本故障不影响其他副本业务IP可达
编号：BIP-TC-013
预置条件：
1、Gateway以多副本方式部署，每个副本均有独立业务IP。
2、所有业务IP访问测试业务路径均正常。
3、具备模拟单个Gateway副本故障的权限。
测试步骤：
1、记录所有副本业务IP和访问结果。
2、模拟其中一个Gateway副本异常重建或短暂不可用。
3、在故障窗口内分别访问其他副本业务IP。
4、故障副本恢复后再次访问其原业务IP。
预期结果：
1、故障前所有业务IP均返回HTTP 200。
2、单副本故障期间，其他副本业务IP仍返回HTTP 200和后端真实响应。
3、故障副本恢复后，其原业务IP恢复HTTP 200。
4、单副本故障不影响其他副本业务IP可达性。

名称：前端网络平面仅暴露业务IP后端网络不直接对外暴露
编号：BIP-TC-014
预置条件：
1、Gateway已同时挂载前端和后端网络平面。
2、业务IP已对外开放，测试业务路径可访问。
3、已知后端网络平面内部服务地址或内部管理端口不应对外开放。
测试步骤：
1、从外部客户端通过业务IP访问测试业务路径，记录HTTP响应。
2、从外部客户端尝试访问后端网络平面内部服务地址或内部管理端口。
3、从外部客户端尝试通过业务IP访问Gateway内部管理路径或非业务路径。
预期结果：
1、测试业务路径返回HTTP 200和后端真实响应。
2、后端网络平面内部服务不可从外部直接访问。
3、Gateway内部管理路径或非业务路径不会通过业务IP暴露内部能力。

名称：业务IP绑定恢复无需人工干预
编号：BIP-TC-015
预置条件：
1、Gateway已部署并获得业务IP，测试业务路径访问正常。
2、容器平台支持Pod重建后自动恢复原业务IP。
3、具备触发Pod重建或节点漂移的权限。
测试步骤：
1、记录业务IP和测试路径访问结果。
2、触发Pod重建或节点漂移。
3、在恢复时间窗口内周期性使用原业务IP访问测试路径。
4、记录恢复过程中是否需要人工重新配置业务IP。
预期结果：
1、故障前业务IP返回HTTP 200和后端真实响应。
2、业务IP由容器平台自动恢复绑定，不需要人工重新配置。
3、恢复完成后原业务IP继续返回HTTP 200。
4、恢复时间满足产品可靠性要求。
