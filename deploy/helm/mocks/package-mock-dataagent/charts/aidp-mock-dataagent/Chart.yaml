apiVersion: v2
name: aidp-mock-dataagent
description: Mock DataAgent backend for AIDP IAM. Wraps mock-dataagent Flask image + HTTPRoute + ReferenceGrant + SecurityPolicy + EnvoyExtensionPolicy in a single Helm release.
type: application
version: 1.0.0
appVersion: "1.0.0"
