# ============================================================================
# aidp-mock-memory — MemoryBank mock backend
# ============================================================================
namespace: mock-memory
gatewayNamespace: aidp-gateway
image: mock-memory:v1
pullPolicy: IfNotPresent
replicas: 1
port: 8080
pathPrefix: /MemoryBank
