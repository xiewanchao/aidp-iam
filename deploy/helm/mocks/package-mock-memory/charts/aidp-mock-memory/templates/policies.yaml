{{/*
SecurityPolicy: bind mock-memory route to ext_authz (pep-proxy in aidp-iam ns).

EnvoyExtensionPolicy: bind mock-memory route to ext_proc (resource-sync, aidp-iam ns)
for ACL auto-sync — PUT 201 on Instances/Memories/Templates → write owner ACL;
DELETE → cascade. Streamed body mode required so resource-sync can read response body.
*/}}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: mock-memory-extauthz
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-memory.labels" . | nindent 4 }}
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: mock-memory-route
  extAuth:
    grpc:
      backendRefs:
      - name: pep-proxy
        namespace: aidp-iam
        port: 9000
    failOpen: false
    bodyToExtAuth:
      maxRequestBytes: 8192
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: mock-memory-extproc
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-memory.labels" . | nindent 4 }}
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: mock-memory-route
  extProc:
  - backendRefs:
    - name: resource-sync
      namespace: aidp-iam
      port: 8082
    processingMode:
      request:
        body: Streamed
      response:
        body: Streamed
    failOpen: true
