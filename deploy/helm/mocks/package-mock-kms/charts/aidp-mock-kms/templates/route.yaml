apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mock-kms-route
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-kms.labels" . | nindent 4 }}
spec:
  parentRefs:
  - name: eg
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: {{ .Values.pathPrefix }}
    backendRefs:
    - name: mock-kms
      namespace: {{ .Values.namespace }}
      port: {{ .Values.port }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mock-kms-health-route
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-kms.labels" . | nindent 4 }}
spec:
  parentRefs:
  - name: eg
  rules:
  - matches:
    - path:
        type: Exact
        value: {{ .Values.pathPrefix }}/health
    backendRefs:
    - name: mock-kms
      namespace: {{ .Values.namespace }}
      port: {{ .Values.port }}
