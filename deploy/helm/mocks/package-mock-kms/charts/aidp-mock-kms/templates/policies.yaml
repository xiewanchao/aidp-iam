{{/*
SecurityPolicy: bind mock-kms route to ext_authz (pep-proxy in aidp-iam ns).

EnvoyExtensionPolicy: bind mock-kms route to ext_proc (resource-sync, aidp-iam ns)
for ACL auto-sync — PUT 201 on KnowledgeBases → write owner ACL (allow_create_without_acl);
DELETE → cascade. Streamed body mode required so resource-sync can read response body.
*/}}
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: SecurityPolicy
metadata:
  name: mock-kms-extauthz
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-kms.labels" . | nindent 4 }}
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: mock-kms-route
  extAuth:
    grpc:
      backendRefs:
      - name: pep-proxy
        namespace: aidp-iam
        port: 9000
    failOpen: false
    bodyToExtAuth:
      maxRequestBytes: 8192
---
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: mock-kms-extproc
  namespace: {{ .Values.gatewayNamespace }}
  labels:
    {{- include "aidp-mock-kms.labels" . | nindent 4 }}
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: mock-kms-route
  extProc:
  - backendRefs:
    - name: resource-sync
      namespace: aidp-iam
      port: 8082
    processingMode:
      request:
        body: Streamed
      response:
        body: Streamed
    failOpen: true
