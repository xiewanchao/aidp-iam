{{/*
keycloak-init Job: bootstraps the aidp realm + writes the aidp-client
Secret into the iam-services namespace.

ttlSecondsAfterFinished=300 auto-cleans the completed Pod after 5 min
so it doesn't sit on a maxPods slot.
*/}}
{{- $keycloakNs := include "keycloak.namespace" . -}}
{{- $iamNs := include "keycloak.iamNamespace" . -}}
{{- $keycloakInit := default dict .Values.keycloakInit -}}
{{- $casRedirectUris := default (list "*") $keycloakInit.casRedirectUris -}}
{{- $webRedirectUris := default (list "/auth/login/callback") $keycloakInit.webRedirectUris -}}
{{- $webPostLogoutRedirectUris := default (list "/auth/login") $keycloakInit.webPostLogoutRedirectUris -}}
{{- $webOrigins := default (list "+") $keycloakInit.webOrigins -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: keycloak-init-sa
  namespace: {{ $keycloakNs }}
automountServiceAccountToken: true
---
# Existing role: write Secrets inside the Keycloak namespace itself.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: keycloak-init-secret-role
  namespace: {{ $keycloakNs }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: keycloak-init-secret-binding
  namespace: {{ $keycloakNs }}
subjects:
- kind: ServiceAccount
  name: keycloak-init-sa
  namespace: {{ $keycloakNs }}
roleRef:
  kind: Role
  name: keycloak-init-secret-role
  apiGroup: rbac.authorization.k8s.io
---
# Cross-namespace role allowing init Job to write the aidp-client Secret
# into the iam-services namespace where the app consumes it.
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: keycloak-init-client-secret-writer
  namespace: {{ $iamNs }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "create", "update", "patch", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: keycloak-init-client-secret-writer
  namespace: {{ $iamNs }}
subjects:
- kind: ServiceAccount
  name: keycloak-init-sa
  namespace: {{ $keycloakNs }}
roleRef:
  kind: Role
  name: keycloak-init-client-secret-writer
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: Job
metadata:
  name: keycloak-init-{{ .Release.Revision }}
  namespace: {{ $keycloakNs }}
  labels:
    app: keycloak
    component: init-job
    release-revision: {{ .Release.Revision | quote }}
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  backoffLimit: 4
  ttlSecondsAfterFinished: 300            # auto-GC the completed Pod after 5 min
  template:
    spec:
      serviceAccountName: keycloak-init-sa
      automountServiceAccountToken: true
      restartPolicy: OnFailure
      containers:
      - name: keycloak-init
        image: "{{ .Values.keycloakInit.image.repository }}:{{ .Values.keycloakInit.image.tag }}"
        imagePullPolicy: {{ .Values.keycloakInit.image.pullPolicy }}
        env:
        - name: KEYCLOAK_URL
          value: "http://keycloak:8080"
        - name: KEYCLOAK_HEALTH_URL
          value: "http://keycloak:9000"
        - name: ADMIN_USER
          value: "{{ .Values.keycloak.admin.username }}"
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: keycloak-credentials
              key: admin-password
        - name: AIDP_REALM
          value: "{{ .Values.keycloakInit.realm }}"
        - name: AIDP_CLIENT_ID
          value: "{{ .Values.keycloakInit.clientId }}"
        - name: AIDP_WEB_CLIENT_ID
          value: "{{ .Values.keycloakInit.webClientId }}"
        - name: AIDP_CAS_CLIENT_ID
          value: {{ default "oms-cas" $keycloakInit.casClientId | quote }}
        - name: AIDP_CAS_REDIRECT_URIS
          value: {{ $casRedirectUris | join "," | quote }}
        - name: AIDP_LOGIN_THEME
          value: {{ default "password-reset-confirm" $keycloakInit.loginTheme | quote }}
        - name: AIDP_WEB_REDIRECT_URIS
          value: {{ $webRedirectUris | join "," | quote }}
        - name: AIDP_WEB_POST_LOGOUT_REDIRECT_URIS
          value: {{ $webPostLogoutRedirectUris | join "," | quote }}
        - name: AIDP_WEB_WEB_ORIGINS
          value: {{ $webOrigins | join "," | quote }}
        - name: K8S_SECRET_NAME
          value: {{ default "keycloak-aidp-client" $keycloakInit.secretName | quote }}
        - name: K8S_NAMESPACE
          value: {{ $iamNs | quote }}
        - name: AIDP_RELEASE_REVISION
          value: "{{ .Release.Revision }}"
        - name: AIDP_ADMIN_USER
          value: {{ default "admin" $keycloakInit.adminUser | quote }}
        - name: AIDP_ADMIN_PASSWORD
          value: {{ default "Admin@123" $keycloakInit.adminPassword | quote }}
        - name: AIDP_NORMAL_USER
          value: {{ default "normal-user" $keycloakInit.normalUser | quote }}
        - name: AIDP_NORMAL_PASSWORD
          value: {{ default "NormalUser@123" $keycloakInit.normalPassword | quote }}
        - name: IAM_DB_URL
          value: {{ include "keycloak.iamDbUrl" . | quote }}
        resources:
          limits:
            cpu: "100m"
            memory: "256Mi"
          requests:
            cpu: "50m"
            memory: "128Mi"
---
apiVersion: batch/v1
kind: Job
metadata:
  name: keycloak-init-secret-cleanup
  namespace: {{ $keycloakNs }}
  labels:
    app: keycloak
    component: cleanup-job
    {{- include "keycloak.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 300
  template:
    spec:
      serviceAccountName: keycloak-init-sa
      automountServiceAccountToken: true
      restartPolicy: OnFailure
      containers:
      - name: keycloak-init-secret-cleanup
        image: "{{ .Values.keycloakInit.image.repository }}:{{ .Values.keycloakInit.image.tag }}"
        imagePullPolicy: {{ .Values.keycloakInit.image.pullPolicy }}
        env:
        - name: TARGET_NAMESPACE
          value: {{ $iamNs | quote }}
        - name: TARGET_SECRET
          value: {{ default "keycloak-aidp-client" $keycloakInit.secretName | quote }}
        command: ["python", "-c"]
        args:
        - |
          import os
          from kubernetes import client, config
          from kubernetes.client.rest import ApiException

          namespace = os.environ["TARGET_NAMESPACE"]
          secret = os.environ["TARGET_SECRET"]

          config.load_incluster_config()
          api = client.CoreV1Api()
          try:
              api.delete_namespaced_secret(secret, namespace)
              print(f"deleted secret {namespace}/{secret}")
          except ApiException as exc:
              if exc.status == 404:
                  print(f"secret {namespace}/{secret} already absent")
              else:
                  raise
