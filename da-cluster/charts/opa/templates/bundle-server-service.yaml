apiVersion: v1
kind: Service
metadata:
  name: bundle-server
  namespace: {{ .Release.Namespace }}
  labels:
    app: pep-proxy
    {{- include "opa.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    name: http
  selector:
    app: pep-proxy
