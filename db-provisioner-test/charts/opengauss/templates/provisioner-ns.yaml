{{- /* Pre-create every target application namespace so Secrets can land there. */ -}}
{{- range .Values.databases }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .secretNamespace }}
  annotations:
    helm.sh/resource-policy: keep
{{- end }}
