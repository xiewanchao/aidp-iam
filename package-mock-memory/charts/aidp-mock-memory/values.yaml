# ============================================================================
# aidp-mock-memory values
# ============================================================================
# pathPrefix MUST stay /memory to match apps.memory.path_prefix=/memory/.
# ============================================================================

namespace: mock-memory
image: mock-memory:v1
pullPolicy: IfNotPresent
replicas: 1
port: 8082
pathPrefix: /memory
