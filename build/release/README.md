# Release Build Helpers

This directory is reserved for release packaging scripts.

Current local deployment and validation scripts live in `deploy/scripts/`.
Gateway openEuler image packaging lives in `build/openeuler/gateway/`.
